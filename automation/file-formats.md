# Форматы файлов SimInTech

## Схемы и проекты

| Расширение | Что это | Читается текстом |
|-----------|---------|------------------|
| `.prt` | Проект (схема) — основной формат | Нет, бинарный |
| `.xprt` | Тот же проект, выгруженный в XML | Да |
| `.pak` | Пакет проектов: несколько схем в одном окне | Да |
| `.mgr` | Графические элементы: окна анимации, панели | Нет |

**Внимание:** расширения `.prj` у проектов SimInTech **нет**. В ранних
материалах этой библиотеки оно упоминалось ошибочно. Проект — это `.prt`,
а его текстовое представление — `.xprt`.

### `.prt` — проект

Бинарный формат, текстовым редактором не правится. Единственный надёжный
способ получить его содержимое — выгрузить в `.xprt` средствами самого
SimInTech (макросом или через COM-интерфейс), а после правки собрать
обратно.

### `.xprt` — проект в XML

Выгружается методом `SaveProjectXML` (COM) или командой `dbexporttoxml`
для базы сигналов. Свойства, которые нужно знать при разборе:

**Кодировка — UTF-8 с BOM.** Так пишет `SaveProjectXML` (проверено на
настоящих выгрузках: BOM `EF BB BF` и объявление `encoding="utf-8"` в самом
XML). cp1251 может попасться от старых версий, поэтому кодировку выбирают по
содержимому — `simintech_api.catalog.decode_xprt`.

Читать `.xprt` как cp1251 нельзя: кириллица превратится в мусор **молча** —
cp1251 декодирует любые байты, и ошибки не будет. `errors="replace"` тоже не
выход: он прячет порчу файла вместо того, чтобы о ней сообщить.

**Строковые значения обёрнуты в обратные кавычки.** В XML имя объекта
хранится не как `Сумматор_1`, а как `` `Сумматор_1` ``. При разборе
бэктики нужно снимать. То же правило действует и для XML базы сигналов
(см. `signal-db.md`).

**Тело скрипта кодируется числовыми эскейпами.** Код блока «Язык
программирования» лежит в свойстве `Script`, где служебные символы
заменены на `#<код>`:

| Эскейп | Символ |
|--------|--------|
| `#13#10` | перевод строки (CR LF) |
| `#9` | табуляция |
| `#60#62` | `<>` |

**Графические объекты лежат вперемешку с блоками.** При извлечении списка
блоков из `.xprt` нужно отфильтровать элементы оформления: `Wire`, `Arc`,
`Line`, `Point`, `PolyLine`, `Polygon`, `Rect`, `Rectangle`, `RotatedText`,
`TextLabel`, `Button`, `Комментарий` и подобные. Иначе в списке сигналов
окажутся подписи и линии связи.

### `.pak` — пакет проектов

Текстовый файл в кодировке UTF-8 **с BOM** — так записаны 62 файла из 63 в
поставке; оставшийся один (`KBA - промконтур охлаждения ядерного реактора/
kba_with_safety.pak`) — CP1251 **без** BOM. Читать надо терпимо к обоим
вариантам: иначе на этом файле имена проектов превратятся в мусор.

Содержит пять секций:

```
[Form]      геометрия окна: Rect.*, ConsoleHeight, WindowState,
            ColCount, ColWidth0..6
[Common]    Synchronize, RelativePath, IsRealTime, TimeMash (float!),
            fSimpleSignalLoad, formstyle, restartname, notqueryremoveforall
[Files]     Count=N, затем 0=, 1=, … — пути к проектам в порядке запуска
[Active]    0=, 1=, … — проект участвует в расчёте
[TimeSync]  0=, 1=, … — проект участвует в синхронизации модельного времени
```

Разбор по байтам 63 файлов поставки дал три ловушки, которых нет в справке
(она не описывает структуру `.pak` вовсе — в официальной таблице расширений
SimInTech этого расширения нет):

* **В `[Files]` лежат только `.prt`** — 167 записей на 63 пакета, ни одного
  `.xprt`. Прежняя редакция этого раздела показывала в примере
  `1=project_b.xprt`; такого в поставке не встречается.
* **`TimeSync` — имя секции, а не ключ**: ключа `TimeSync=` не существует
  ни в одном файле, внутри секции строки `0=`, `1=`.
* **`ColCount` есть и здесь, и в `.tbl`** (`$ColCount`), но означает разное:
  в `.pak` — ширину колонок окна, в `.tbl` — число столбцов матрицы.

Секция `[Form]` есть у всех 63 файлов, но у `kba_with_safety.pak` она короче
(только `Rect.*`) — отсутствие ключа не ошибка, а сокращённая запись.

Пакет нужен для совместного расчёта нескольких схем — например, когда
модель объекта и модель регулятора лежат в разных проектах.

## База сигналов

| Расширение | Что это |
|-----------|---------|
| `.db` | База сигналов, бинарный формат |
| `.xml` | Та же база, выгруженная в XML |

Подробнее — в `signal-db.md`.

## Кодогенерация

| Расширение | Что это |
|-----------|---------|
| `.alt` | Конфигурация кодогенератора в C |
| `.inc` | Сгенерированный C-код, а также тела скриптов |
| `.h` | Заголовочный файл сгенерированного модуля |
| `_init.inc`, `_state.inc` | Инициализация и хранение состояний модели |
| `.extvars.table`, `.intvars.table` | Таблицы внешних и внутренних переменных |

### Конфигурация `.alt`

Задаёт, какие проекты генерировать и с какими параметрами. Основные секции:

- `[filelist]` — перечень модулей: путь к `.prt`, имя алгоритма (`algoname`),
  период вызова, число вызовов, имя выходного модуля. Количество задаётся
  ключом `count`, дальше идут пронумерованные строки `itemN`, `algonameN`,
  `periodN`, `callcountN`, `exenameN`.
- `[debug]` — параметры генерации: каталог исходников, шаблон сборки,
  `encoding`, `translit_style`, `header_style`, `f_global_divide_check`.

Отсюда два важных следствия. Во-первых, `translit_style` и `encoding`
определяют, как русские имена превратятся в идентификаторы C — при
рассогласовании имён сборочный конвейер не найдёт символы. Во-вторых,
`f_global_divide_check` включает проверку деления на ноль на уровне
кодогенератора: это тот случай, когда ошибку ловит сама генерация,
а не анализ в блоке (см. `language/pitfalls.md`).

### Признак успешной генерации

После кодогенерации для каждого алгоритма должен появиться полный набор
файлов: `.h`, `.inc`, `_init.inc`, `_state.inc`, `.extvars.table`,
`.intvars.table`. Отсутствие любого из них — признак сбоя, даже если
код возврата процесса был нулевым.

## Библиотеки блоков

Библиотеки блоков — файлы `.csl` в каталоге `bin` установленного SimInTech.

## Таблицы: `.tbl`

Файл таблиц с данными (`bin/DataBase/**`, 503 файла в поставке; также
встречается в `Demo`). Заголовок из директив, затем числовая матрица:

```
$GlobalScript=×N   $Caption=   $TabType=   $GraphType=
$ColCount=   $RowCount=   $MatrixName=   $Script=×N
```

Ловушки, проверенные на файлах:

* **Директив восемь**, а не шесть: помимо `$Caption`, `$TabType`, `$ColCount`,
  `$RowCount`, `$MatrixName`, `$Script` есть ещё `$GraphType` и
  `$GlobalScript` (последний — многострочный, как `$Script`).
* **В одном файле бывает несколько таблиц** — их разделяет строка из решёток
  (37 символов в 397 случаях, 40 в 77). До восьми таблиц в одном файле.
* **`$MatrixName` не всегда `M`**: встречаются `kpd`, `h`, `KSI`, `Hn`.
* **`.tbl` бывает не таблицей**: 24 файла без единой `$`-директивы (это файлы
  языка SimInTech), а 4 файла — бинарные (до 191 МБ: счётчик и массив `double`).
* **Подписей осей нет** — только числа; текст живёт в `$Caption` и в
  комментариях `$Script=//…`.
* Формат описан **не справкой** (в ней структуры нет вовсе), а исходником
  поставки `source/Root/tbls.pas`.
* Справка SimInTech о структуре `.tbl` молчит — это не «описывает неверно».

## Иконки: `.saraface` и `images_mvtu`

**Не путать два механизма.**

* **`.saraface`** (`bin/Faces/`, 21 файл) — векторные картинки элементов модуля
  анализа надёжности **SARA**: `Id` (GUID), `Name` (код картинки: `PUMP`,
  `VALVE`, `CIRCLE`, `1`, `2`), `Width`, `Height`, `Elements[]` с примитивами
  `Line`/`Arc`/`Curve` (координаты — пиксели, ось Y вниз, цвета — 24-битный
  RGB в виде целого). Среди имён нет ни одного класса блоков SimInTech: это
  картинки SARA, и связь «класс блока → файл» из них не выводится.
* **Иконки классов блоков** — каталог `bin/images_mvtu/` (около 450 файлов
  `.png`/`.gif.png`), подключается в профиле (`base.xml`), а класс ссылается на
  картинку свойствами `VisibleIcon`, `ColorIcon`, `ImageType` из каталога
  параметров. Именно это, а не `.saraface`, даёт геометрию блока.

## Итог по кодировкам

| Файл | Кодировка |
|------|-----------|
| `.xprt` | UTF-8 (с BOM) |
| База сигналов в XML | UTF-8 (с BOM); cp1251 — в старых выгрузках |
| Значения в XML | кавычки двух конвенций: бэктики или одинарные |
| `.pak` | UTF-8 (с BOM); один файл в поставке — cp1251 без BOM |
| `.dblocalconf`, `.dbconf` | UTF-8 (с BOM) |
| `.tbl` | UTF-8 с BOM (171 файл) или cp1251 без BOM (305) |
| `.saraface` | UTF-8 без BOM (JSON) |
| `.prt`, `.mgr`, `.db` | бинарные |
| Файлы макросов | не унифицировано — встречаются cp1251 и UTF-8 |

Разнобой кодировок — источник большинства ошибок при автоматизации.
Если кириллица превратилась в мусор, первым делом проверяйте кодировку
файла, а не логику разбора.
