"""Исключения библиотеки simintech_api."""

from typing import Optional


class SimInTechError(Exception):
    """Базовое исключение библиотеки."""


class ComConnectionError(SimInTechError):
    """Не удалось подключиться к COM-серверу SimInTech (mmain.exe)."""


class ComCallError(SimInTechError):
    """Ошибка вызова метода COM-сервера.

    Args:
        method: имя вызванного метода.
        hr: HRESULT, если был получен.
        message: текст ошибки.
    """

    def __init__(self, method: str, hr: Optional[int] = None,
                 message: str = ""):
        self.method = method
        self.hr = hr
        self.message = message
        detail = f" [{hr:#010x}]" if hr is not None else ""
        super().__init__(f"COM-вызов {method} завершился ошибкой{detail}: {message}")


class ProjectError(SimInTechError):
    """Ошибка при работе с проектом/пакетом (создание, открытие, сохранение)."""


class PageError(SimInTechError):
    """Ошибка при работе со страницей."""


class BlockError(SimInTechError):
    """Ошибка при работе с блоком (создание, свойства, порты)."""


class UnsupportedBlockError(BlockError):
    """Класс блока **отвергает библиотека**, а не COM.

    `CreateBlock` запись такого блока создаёт: измерено 2026-09-18 — «Из памяти»
    появляется как `FromMem_0` (свойства `a`/`src_type`, портов нет), «Порт
    выхода» — как `PortConnector_0` (один порт), обе записи видны в выгрузке.
    Отказывает `Page.create_block`, потому что годность этих блоков **в расчёте**
    не проверена.

    Разница для вызывающего существенна: «создать нельзя» и «создать можно, но
    работать не доказано» — разные состояния, и прежняя формулировка отправляла
    искать причину в COM, где её нет.
    """


class PortError(SimInTechError):
    """Ошибка при работе с портом."""


class WireError(SimInTechError):
    """Ошибка при работе с линией связи."""


class SignalError(SimInTechError):
    """Ошибка при работе с сигналом (не найден, неверный тип, чтение/запись)."""


class SimulationError(SimInTechError):
    """Ошибка управления расчётом."""


class LayoutError(SimInTechError):
    """Ошибка алгоритмов размещения/трассировки (нет пути, конфликт)."""


class PackError(SimInTechError):
    """Ошибка при работе с пакетом проектов (`.pak`)."""


class ScriptBridgeError(SimInTechError):
    """Не удалось выполнить скрипт в проекте или получить его результат.

    Отдельный класс нужен потому, что причину отказа среда не сообщает:
    различить «скрипт оборвался» и «скрипт не скомпилировался» можно только по
    косвенным признакам — маркеру завершения в файле результата и росту
    модельного времени, — и оба признака названы в тексте исключения.

    **Признак не равен причине.** Неподвижное модельное время означает лишь,
    что расчёт не пошёл, а не почему: так же выглядит модель, которая
    структурно не считает (блок с неподключённым входом останавливает расчёт
    всей модели). Измерено 2026-09-23. Поэтому текст отказа называет состояние
    и не выбирает между версиями, пока нет независимого признака причины.
    """


class ScriptBridgeUnsafeStateError(ScriptBridgeError):
    """Состояние скриптов страниц не установлено: восстанавливать некуда.

    Отдельный класс, **дочерний** `ScriptBridgeError`, а не независимый:
    `except ScriptBridgeError` обязан по-прежнему ловить отказ моста целиком,
    иначе правка сломает вызывающий код. А отдельный тип позволяет отличить
    именно этот случай — «в проекте, возможно, остался пробный скрипт, а
    автоматическое восстановление не было безопасным» — и сказать об этом
    пользователю, не разбирая текст сообщения.
    """


class TopologyError(SimInTechError):
    """Строка протокола топологии не соответствует грамматике.

    Отдельный класс, а не `ValueError`: это отказ разбора данных, пришедших из
    среды, и вызывающий должен отличать его от ошибок собственного кода.
    """
